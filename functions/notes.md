* __*args__  : Are used for passing the arguments to the functions. Multiple arguments can be passed once using *args in a function's parameter.

* __**kwargs__ : It is also used for passing arguments in in functions but it passes any number of keyword arguments i.e, key:value to the function. 